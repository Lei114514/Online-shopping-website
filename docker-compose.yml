services:
  mysql:
    image: mysql:8.0
    container_name: online-shop-mysql
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: online_shop_db
      MYSQL_USER: onlineshop
      MYSQL_PASSWORD: onlineshop123
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./docs/database-schema.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - online-shop-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 10

  app:
    build: .
    container_name: online-shop-app
    depends_on:
      mysql:
        condition: service_healthy
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/online_shop_db?useSSL=false&allowPublicKeyRetrieval=true
      SPRING_DATASOURCE_USERNAME: onlineshop
      SPRING_DATASOURCE_PASSWORD: onlineshop123
      SPRING_JPA_HIBERNATE_DDL_AUTO: update
      # 郵件配置 - 請替換為您的 163 郵箱和授權碼
      MAIL_USERNAME: ${MAIL_USERNAME:-your-email@163.com}
      MAIL_PASSWORD: ${MAIL_PASSWORD:-your-authorization-code}
    ports:
      - "8080:8080"
    volumes:
      - uploads:/app/uploads
      # Mount local images directory to container
      - ./src/main/resources/static/images/products:/app/src/main/resources/static/images/products
    networks:
      - online-shop-network
    restart: unless-stopped

volumes:
  mysql_data:
    driver: local
  uploads:
    driver: local

networks:
  online-shop-network:
    driver: bridge
